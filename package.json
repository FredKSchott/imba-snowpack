{
  "scripts": {
    "prepare": "snowpack",
    "start": "snowpack dev && imbac -w -o public/ src/",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "webDependencies": {
    "imba": "^2.0.0-alpha.45"
  },
  "devDependencies": {
    "snowpack": "^2.0.0-0"
  }
}
